const value = 3 // Change the value from a string to a number
console.log(value + 4 + value)

/*The code logged 343 becuase:
* The values were corhesed into strings
* the values were concatinated instead of added into a sum.
*/